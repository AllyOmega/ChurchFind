# The site runs without any of this -- these are only needed for the API server
# and accounts. The scraper is standard library only.
fastapi>=0.110
uvicorn[standard]>=0.27
argon2-cffi>=23.1

# Tests
pytest>=8.0
httpx>=0.27
