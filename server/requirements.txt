# Development and CI: the runtime dependencies plus the test tooling.
#
# The deployed image installs the root requirements.txt instead, which pins
# exact versions and carries no test packages. This file stays loose because a
# contributor's existing environment should not be a fight.
-r ../requirements.txt

pytest>=8.0
httpx>=0.27
